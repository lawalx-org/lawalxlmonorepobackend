import { Module } from '@nestjs/common';


@Module({
  imports: [
  
    // add more feature modules here
  ],
  exports: [
   
    // export them so other modules can use if needed
  ]
})
export class StoreModule {}
