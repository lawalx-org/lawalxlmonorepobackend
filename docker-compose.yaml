services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    image: sakibsakib/lawalbackendbackend:latest
    profiles:
      - prod
    container_name: lawalbackendbackend3
    restart: always
    env_file:
      - .env
    depends_on:
      - db
      - redis
    volumes:
      - uploads:/app/uploads
      - uploads-file:/app/uploads-file
      - uploads-profile-images:/app/uploads-profile-images
    ports:
      - 2343:2342
    networks:
      - law_network

  db:
    image: postgres:15
    container_name: law_db
    restart: always
    profiles:
      - prod
      - dev
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - law_pgdata_new:/var/lib/postgresql/data
    networks:
      - law_network

  redis:
    image: redis:7
    container_name: law_redis_cache
    restart: always
    profiles:
      - prod
      - dev
    ports:
      - "6380:6379"
    volumes:
      - law_redisdata:/data
    networks:
      - law_network

volumes:
  law_pgdata_new:
  law_redisdata:
  uploads-profile-images:
  uploads-file:
  uploads:

networks:
  law_network:
    driver: bridge